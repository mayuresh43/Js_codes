const accountId = 1254
let accountEmail = "mayuresh@google.com"
var accountPass = "1289" 
let accountState

// prefer not use var bcoz of issue in block scope functional scope.

console.table([accountId, accountEmail, accountPass, accountState])